# Schema name consistent with main app.
spring.jpa.properties.hibernate.default_schema=eduo

# Let Hibernate create the schema from entity classes, same as in dev.
spring.jpa.hibernate.ddl-auto=create-drop

# Prevent data.sql from running in tests — each test manages its own data.
spring.sql.init.mode=never